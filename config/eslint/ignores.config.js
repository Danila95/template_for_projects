// ============================================================================
// КОНФИГУРАЦИЯ ИГНОРИРУЕМЫХ ФАЙЛОВ
// ============================================================================
// Файлы и директории, которые ESLint не должен проверять
// ============================================================================

/**
 * Получить конфигурацию игнорируемых файлов и папок
 * Эти файлы не будут проверяться ESLint
 * @returns {import('./types.js').ESLintConfigItem}
 */
export function getIgnoresConfig() {
	return {
		// Игнорируемые файлы и директории
		ignores: [
			// Директории сборки
			'dist',
			'build',
			'out',

			// Зависимости
			'node_modules',

			// Кэш и временные файлы
			'.cache',
			'*.log',

			// Конфигурация и сборка
			'vite.config.ts.timestamp-*',

			// Покрытие тестов
			'coverage',

			// Дополнительные игнорирования (раскомментируйте при необходимости):
			// '.vscode',
			// '.idea',
			// 'public',
		],
	}
}
